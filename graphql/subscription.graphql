extend type Mutation {
    triggerTestSubscription: Boolean
        @field(
            resolver: "App\\GraphQL\\Mutations\\TriggerTestSubscriptionMutation@resolve"
        )
        @broadcast(subscription: "test")
}

type Subscription {
    test: Int
        @subscription(class: "App\\GraphQL\\Subscriptions\\TestSubscription")
}
